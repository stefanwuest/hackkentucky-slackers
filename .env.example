# Copy to .env for local development.
# Used by scripts/seed-d1.mjs when --db is omitted.
D1_DATABASE_NAME=zywave-prospect-intel-local
D1_DATABASE_BINDING=DB

# Required by apps/api/src/services/model-queries.ts when model calls are used.
OPENROUTER_API_KEY=
